# OS files
.DS_Store
Thumbs.db

# Environment / secrets
.env
*.local

# IDE
.vscode/
.idea/

# Local Cloudflare Wrangler cache/state
.wrangler/

# Claude Code local config — not for the public web root
.claude/

# Internal planning docs — not for the public web root (no build step; repo
# root is served as-is, so anything committed here is publicly fetchable)
handoffs/

# Press release drafts — iterated on locally, not shipped until finalized
press-release-launch.html
